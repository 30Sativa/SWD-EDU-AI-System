namespace EduAISystem.Domain.Enums
{
    public enum SubmissionStatusDomain
    {
        Draft = 0,      // mới tạo (hiếm dùng)
        Submitted = 1,  // đã nộp, chờ chấm
        Graded = 2      // đã chấm điểm
    }
}

